from string import ascii_lowercase, ascii_uppercase

u = DivStr(ascii_uppercase[::-1])
print(* u * 2)
print(* (u * 3)[:-2] // 3)
print(* (u * 3)[:-2] % 3)
print(* (u[:-2] * 3) // 3)
print(* (u[:-2] * 3) % 3)
